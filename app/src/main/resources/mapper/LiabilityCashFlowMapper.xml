<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xl.alm.app.mapper.LiabilityCashFlowMapper">

    <resultMap type="com.xl.alm.app.entity.LiabilityCashFlowEntity" id="LiabilityCashFlowEntityResult">
        <result property="id" column="id"/>
        <result property="accountPeriod" column="account_period"/>
        <result property="cashFlowType" column="cash_flow_type"/>
        <result property="bpType" column="bp_type"/>
        <result property="durationType" column="duration_type"/>
        <result property="designType" column="design_type"/>
        <result property="isShortTerm" column="is_short_term"/>
        <result property="actuarialCode" column="actuarial_code"/>
        <result property="businessCode" column="business_code"/>
        <result property="productName" column="product_name"/>
        <result property="insuranceMainType" column="insurance_main_type"/>
        <result property="insuranceSubType" column="insurance_sub_type"/>
        <result property="cashValSet" column="cash_val_set"/>
        <result property="createTime" column="create_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="isDel" column="is_del"/>
    </resultMap>

    <sql id="selectLiabilityCashFlowEntityVo">
        select id, account_period, cash_flow_type, bp_type, duration_type, design_type, is_short_term, 
               actuarial_code, business_code, product_name, insurance_main_type, insurance_sub_type,
               cash_val_set, create_time, create_by, update_time, update_by, is_del
        from t_dur_liability_cash_flow
    </sql>

    <!-- 查询负债现金流列表 -->
    <select id="selectLiabilityCashFlowEntityList" parameterType="com.xl.alm.app.query.LiabilityCashFlowQuery" resultMap="LiabilityCashFlowEntityResult">
        <include refid="selectLiabilityCashFlowEntityVo"/>
        <where>
            <if test="id != null">and id = #{id}</if>
            <if test="accountPeriod != null and accountPeriod != ''">and account_period = #{accountPeriod}</if>
            <if test="cashFlowType != null and cashFlowType != ''">and cash_flow_type = #{cashFlowType}</if>
            <if test="bpType != null and bpType != ''">and bp_type = #{bpType}</if>
            <if test="durationType != null and durationType != ''">and duration_type = #{durationType}</if>
            <if test="designType != null and designType != ''">and design_type = #{designType}</if>
            <if test="isShortTerm != null and isShortTerm != ''">and is_short_term = #{isShortTerm}</if>
            <if test="actuarialCode != null and actuarialCode != ''">and actuarial_code = #{actuarialCode}</if>
            <if test="businessCode != null and businessCode != ''">and business_code = #{businessCode}</if>
            <if test="productName != null and productName != ''">and product_name like concat('%', #{productName}, '%')</if>
            <if test="insuranceMainType != null and insuranceMainType != ''">and insurance_main_type = #{insuranceMainType}</if>
            <if test="insuranceSubType != null and insuranceSubType != ''">and insurance_sub_type = #{insuranceSubType}</if>
            and is_del = 0
        </where>
        order by create_time desc
    </select>

    <!-- 用id查询负债现金流 -->
    <select id="selectLiabilityCashFlowEntityById" parameterType="Long" resultMap="LiabilityCashFlowEntityResult">
        <include refid="selectLiabilityCashFlowEntityVo"/>
        where id = #{id} and is_del = 0
    </select>

    <!-- 根据条件查询负债现金流 -->
    <select id="selectLiabilityCashFlowEntityByCondition" resultMap="LiabilityCashFlowEntityResult">
        <include refid="selectLiabilityCashFlowEntityVo"/>
        <where>
            <if test="accountPeriod != null and accountPeriod != ''">and account_period = #{accountPeriod}</if>
            <if test="cashFlowType != null and cashFlowType != ''">and cash_flow_type = #{cashFlowType}</if>
            <if test="bpType != null and bpType != ''">and bp_type = #{bpType}</if>
            <if test="durationType != null and durationType != ''">and duration_type = #{durationType}</if>
            <if test="designType != null and designType != ''">and design_type = #{designType}</if>
            <if test="isShortTerm != null and isShortTerm != ''">and is_short_term = #{isShortTerm}</if>
            <if test="actuarialCode != null and actuarialCode != ''">and actuarial_code = #{actuarialCode}</if>
            and is_del = 0
        </where>
        limit 1
    </select>

    <!-- 批量插入负债现金流数据 -->
    <insert id="batchInsertLiabilityCashFlowEntity" parameterType="java.util.List">
        insert into t_dur_liability_cash_flow (
            account_period, cash_flow_type, bp_type, duration_type, design_type, is_short_term, 
            actuarial_code, business_code, product_name, insurance_main_type, insurance_sub_type,
            cash_val_set, create_by, update_by
        ) values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.accountPeriod},
            #{item.cashFlowType},
            #{item.bpType},
            #{item.durationType},
            #{item.designType},
            #{item.isShortTerm},
            #{item.actuarialCode},
            #{item.businessCode},
            #{item.productName},
            #{item.insuranceMainType},
            #{item.insuranceSubType},
            #{item.cashValSet},
            #{item.createBy},
            #{item.updateBy}
            )
        </foreach>
    </insert>

    <!-- 更新负债现金流数据 -->
    <update id="updateLiabilityCashFlowEntity" parameterType="com.xl.alm.app.entity.LiabilityCashFlowEntity">
        update t_dur_liability_cash_flow
        <set>
            <if test="accountPeriod != null and accountPeriod != ''">account_period = #{accountPeriod},</if>
            <if test="cashFlowType != null and cashFlowType != ''">cash_flow_type = #{cashFlowType},</if>
            <if test="bpType != null and bpType != ''">bp_type = #{bpType},</if>
            <if test="durationType != null and durationType != ''">duration_type = #{durationType},</if>
            <if test="designType != null and designType != ''">design_type = #{designType},</if>
            <if test="isShortTerm != null and isShortTerm != ''">is_short_term = #{isShortTerm},</if>
            <if test="actuarialCode != null and actuarialCode != ''">actuarial_code = #{actuarialCode},</if>
            <if test="businessCode != null and businessCode != ''">business_code = #{businessCode},</if>
            <if test="productName != null and productName != ''">product_name = #{productName},</if>
            <if test="insuranceMainType != null and insuranceMainType != ''">insurance_main_type = #{insuranceMainType},</if>
            <if test="insuranceSubType != null and insuranceSubType != ''">insurance_sub_type = #{insuranceSubType},</if>
            <if test="cashValSet != null and cashValSet != ''">cash_val_set = #{cashValSet},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = now()
        </set>
        where id = #{id} and is_del = 0
    </update>

    <!-- 删除指定账期的负债现金流数据 -->
    <update id="deleteLiabilityCashFlowEntityByPeriod" parameterType="String">
        update t_dur_liability_cash_flow set is_del = 1, update_time = now() 
        where account_period = #{accountPeriod} and is_del = 0
    </update>

    <!-- 删除指定id的负债现金流数据 -->
    <update id="deleteLiabilityCashFlowEntityById" parameterType="Long">
        update t_dur_liability_cash_flow set is_del = 1, update_time = now() 
        where id = #{id} and is_del = 0
    </update>
    
    <!-- 批量删除负债现金流 -->
    <update id="deleteLiabilityCashFlowEntityByIds" parameterType="Long">
        update t_dur_liability_cash_flow set is_del = 1, update_time = now()
        where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        and is_del = 0
    </update>
</mapper>
